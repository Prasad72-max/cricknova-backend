class ApiConfig {
  // 🚀 Production backend (Render)
  static const String baseUrl = "https://cricknova-backend.onrender.com";

  // ⏱️ Optional: request timeout (ms)
  static const int timeout = 30000;
}